// TODO (Person 2 — Create Match):
// Build the form here (sport, location, date, time, max players, skill
// level, description) and insert into the `matches` table on submit.
export default function CreateMatch() {
  return (
    <section className="page">
      <h1>Create a Match</h1>
      <p className="page-subtitle">Form fields go here.</p>
    </section>
  );
}
