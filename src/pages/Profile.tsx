// TODO (Person 4 — Account/Profile):
// Build sign-up/login UI and profile editing here, plus lists of the
// current user's joined and created matches.
export default function Profile() {
  return (
    <section className="page">
      <h1>Your Profile</h1>
      <p className="page-subtitle">Sign-up / login and profile details go here.</p>
    </section>
  );
}
